# News Reader Server

Express proxy that securely forwards requests to TheNewsApi.

## Setup

1. Copy `.env.example` to `.env`
2. Set `THENEWSAPI_TOKEN` in `.env`
3. Run `npm install`
4. Run `npm run dev`

Server starts on port 5177.
